description: "Schema requiring a column that doesn't exist"
properties:
  samples:
    type: array
    items:
      type: object
      properties:
        sample_name:
          type: string
        genome:
          type: string
      required:
        - sample_name
        - genome
