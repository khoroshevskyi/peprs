description: "Schema with project-level config validation"
properties:
  description:
    type: string
  pep_version:
    type: string
  samples:
    type: array
    items:
      type: object
      properties:
        sample_name:
          type: string
required:
  - description
